{
  "name": "chambaikiemtra",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Ung dung tao nhieu ma de trac nghiem va xuat PDF/ZIP.",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "build": "npm run build -w backend && npm run build -w frontend",
    "start": "npm run start -w backend"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
