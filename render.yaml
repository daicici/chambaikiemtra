services:
  - type: web
    name: chambaikiemtra
    runtime: node
    plan: free
    buildCommand: npm install && node scripts/generate-omr-template.js && npm run build
    startCommand: npm run start -w backend
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
